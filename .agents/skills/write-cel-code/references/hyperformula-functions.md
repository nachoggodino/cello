# Curated HyperFormula Functions For Cello

Use the npm-bundled skill reference at `skills/write-cel-code/references/hyperformula-functions.md` for the full curated table. This project-local copy exists so the repository skill link resolves when browsing references from `.agents/skills/write-cel-code`.
